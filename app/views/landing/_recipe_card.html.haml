%a{href: recipe_path(recipe),
  title: "#{recipe.title} #{recipe.subtitle}",
  name: 'recipe',
  'data-day' => recipe.day }
  = card do
    .row.card-img-container.carousel
      %img.card-img{src: recipe.photo}
      .carousel-caption
        .row
          .col-xs-11.col-xs-offset-1
            %h3= recipe.title
        .row
          .col-xs-11.col-xs-offset-1
            %h4= recipe.subtitle
        .row
          %center.col-xs-4
            %i.fa.fa-fw.fa-clock-o
            #{recipe.cooking_time} мин.
          %center.col-xs-4
            %strong день #{human_day(recipe.day)}
          %center.col-xs-4
            %i.fa.fa-fw.fa-fire
            #{recipe.calories} ккал.
    .card-description.col-xs-11.col-xs-offset-1
      .row
        .col-xs-7
          Для приготовления понадобится:
        .col-xs-5.text-center
          - recipe.inventory_items.each do |ii|
            = inventory_item_tag ii
