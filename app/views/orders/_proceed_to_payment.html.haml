.row
  %h1 Спасибо за Ваш заказ.
%br/
.row
  .lead
    Чтобы оплатить заказ, перейдите в платёжную систему:
  = form_tag Rails.application.config.yandex_kassa.url do
    = hidden_field_tag :shop_id, Rails.application.config.yandex_kassa.shop_id
    = hidden_field_tag :scid, Rails.application.config.yandex_kassa.scid
    = hidden_field_tag :customerNumber, @order.id
    = hidden_field_tag :paymentType, 'AC'
    = hidden_field_tag :sum, @order.price
    = submit_tag 'ПЕРЕЙТИ К ОПЛАТЕ', class: 'btn btn-primary btn-lg'
  %br/
  %br/
